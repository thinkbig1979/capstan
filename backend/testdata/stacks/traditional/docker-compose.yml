version: '3.8'

services:
  web:
    image: nginx:1.21
    restart: unless-stopped
    ports:
      - "80:80"

  db:
    image: postgres:14
    restart: unless-stopped
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=mydb

volumes:
  db_data:
